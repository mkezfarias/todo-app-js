---
linters:
    flake8:
    eslint:
        config: './eslint.config'
    csslint:
        ignore: 'box-model,ids,scroll-behavior,order-alphabetical'

files:
    ignore:
        - 'src/generated/*'
        - 'webroot/bower_components/*'